[pytest]
testpaths = tests
# tests/conftest.py redirects every store into a scratch directory before
# bot.config is imported, so the suite never touches the live JSON files or
# reads the real .env. See its module docstring.
addopts = -q
filterwarnings =
    ignore::DeprecationWarning:discord.player

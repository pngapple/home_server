# Installed to /etc/systemd/journald.conf.d/persistent.conf by
# scripts/install_services.sh.
#
# Default Storage=volatile throws away all logs on reboot, which is exactly
# when you need `journalctl -u <service> -b -1` to see why something didn't
# come up. Persist them instead.
[Journal]
Storage=persistent
